{
  "name": "@dump-flow/db",
  "version": "0.1.0",
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "generate": "prisma generate",
    "push": "prisma db push",
    "migrate": "prisma migrate dev",
    "seed": "tsx --env-file=.env seed.ts",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@prisma/client": "^5.14.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "prisma": "^5.14.0",
    "typescript": "^5.9.2"
  }
}
