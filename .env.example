# Variaveis de ambiente necessárias para rodar o projeto

#OBS: Variaveis sem valor, apenas para fins de instrução das chaves e conexões necessárias

# Connect to Postgres via the shared transaction-mode pooler (IPv4-only)
DATABASE_URL=""

# Connect to Postgres via the shared session-mode pooler (used for migrations)
DIRECT_URL=""